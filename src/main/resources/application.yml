server:
  port: 8888
  
management:
  security:
    enabled: false
  
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vanddang/oksusu-config-server
          search-paths: config